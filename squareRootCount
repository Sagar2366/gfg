#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main() {
    int number=8;
    for(int i=0;i<(number/2);i++)
    {
        if(pow(2,i)==number)
            cout<<i;
    }
	return 0;
}

Output:
3
